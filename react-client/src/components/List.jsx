/*import React from 'react';
import SearchBar from './SearchBar.jsx';

const List = (props) => (
  <div>
    <h4> List Component </h4>
    There are { props.items.length } items.
    { props.items.map(item => <SearchBar item={item}/>)}
  </div>
)

export default List;*/